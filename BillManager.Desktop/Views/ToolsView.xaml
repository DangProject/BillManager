<UserControl x:Class="BillManager.Desktop.Views.ToolsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" d:DesignHeight="200" d:DesignWidth="700">

    <Grid Background="{StaticResource SlateBlueLightGrade1}">
        <TextBlock Text="{Binding Source={x:Static sys:DateTime.Now},StringFormat='D'}"
                   Margin="10"
                   FontWeight="Bold" FontSize="24" Foreground="White"/>
        <!--<Button Content="Add New Bill" 
                Command="{Binding NavigateToAddBillCommand}"
                Height="30" Width="100" Margin="10"
                HorizontalAlignment="Right" VerticalAlignment="Top"/>-->
    </Grid>
</UserControl>
